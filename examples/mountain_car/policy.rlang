import vocab.json

Action go_right := 2
Action go_left := 0
Action no_op := 1

Factor position := S[0]
Factor velocity := S[1]

Policy gain_momentum:
    if velocity <= 0:
        Execute go_left
    else:
        Execute go_right