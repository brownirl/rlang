import vocab.json

Action go_right := 2
Action go_left := 0
Action no_op := 1

Factor position := S[0]
Factor velocity := S[1]

Policy main:
    if velocity < 0:
        go_left
    else:
        go_right