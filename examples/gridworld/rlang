Feature position := S[0,1]
Feature x := position[0]
Feature y := position[1]

Predicate reached_goal := x == 5 and y == 1
Predicate reached_wall := x == 3 and y == 1
Predicate in_lava := position in lava_locs

# define action

Action up := [0, 1]
Action down := 
Action left :=
Action right :=

Effect A == up:
    position += [0,1]

Effect A == down:
    position += [0,-1]

Effect A == left:
    position += [-1,0]

Effect A == right:
    position += [0,1]

